model:
  base_learning_rate: 1e-5
  target: cldm.cldmbase.ControlLDM
  params:
    noisy_prex: True
    # -----
    sd_locked: True
    linear_start: 0.00085
    linear_end: 0.0120
    num_timesteps_cond: 1
    log_every_t: 200
    timesteps: 1000
    cond_stage_key: "txt"
    preframe_key: "src_frame" # previous video frames
    first_stage_key: "tar_frame" # current video frame
    image_size: 32
    channels: 4
    cond_stage_trainable: false
    conditioning_key: crossattn
    monitor: val/loss_simple_ema
    scale_factor: 0.18215
    use_ema: False
    only_mid_control: False
    learn_logvar: False

    preframe_config:
      target: cldm.cldmbase.PreframeProcessNet
      params:
        use_checkpoint: True
        image_size: 32 # unused
        in_channels: 4
        prex_channels: 4
        model_channels: 320
        attention_resolutions: [ 4, 2, 1 ]
        num_res_blocks: 2
        channel_mult: [ 1, 2, 4, 4 ]
        num_head_channels: 64 # need to fix for flash-attn
        use_spatial_transformer: True
        use_linear_in_transformer: True
        transformer_depth: 1
        context_dim: 1024
        legacy: False

    unet_config:
      target: cldm.cldmbase.ControlledUnetModel
      params:
        use_checkpoint: True
        image_size: 32 # unused
        in_channels: 4
        out_channels: 4
        model_channels: 320
        attention_resolutions: [ 4, 2, 1 ]
        num_res_blocks: 2
        channel_mult: [ 1, 2, 4, 4 ]
        num_head_channels: 64 # need to fix for flash-attn
        use_spatial_transformer: True
        use_linear_in_transformer: True
        transformer_depth: 1
        context_dim: 1024
        legacy: False

    first_stage_config:
      target: ldm.models.autoencoder.AutoencoderKL
      params:
        embed_dim: 4
        monitor: val/rec_loss
        ddconfig:
          #attn_type: "vanilla-xformers"
          double_z: true
          z_channels: 4
          resolution: 256
          in_channels: 3
          out_ch: 3
          ch: 128
          ch_mult:
          - 1
          - 2
          - 4
          - 4
          num_res_blocks: 2
          attn_resolutions: []
          dropout: 0.0
        lossconfig:
          target: torch.nn.Identity

    cond_stage_config:
      target: ldm.modules.encoders.modules.FrozenOpenCLIPEmbedder
      params:
        freeze: True
        layer: "penultimate"

data:
  target: main.DataModuleFromConfig
  params:
    wrap: False
    batch_size: 8 # for debug
#    batch_size: 48
    num_workers: 4
    train:
      target: cldm.data.custom.CustomDataset_1to1
      params:
        size: 256
        flip_p: 0.5
        max_data_num: 160 # for debug
        root: /home/zhongguokexueyuanzidonghuayanjiusuo/datasets/UCF-101/frames_fps20
    validation:
      target: cldm.data.custom.CustomDataset_1to1
      params:
        size: 256
        flip_p: 0.5
#        max_data_num: 2048
        max_data_num: 160 # for debug
        root: /home/zhongguokexueyuanzidonghuayanjiusuo/datasets/UCF-101/frames_fps20

lightning:
  trainer:
    ckpt_load_strict: False
    accelerator: 'gpu'
    devices: 4
    log_gpu_memory: all
    max_epochs: 500
    precision: 16
    auto_select_gpus: False
    strategy:
      target: strategies.DDPStrategy
      params:
        find_unused_parameters: True
#    strategy:
#      target: strategies.ColossalAIStrategy
#      params:
#        use_chunk: True
#        enable_distributed_storage: True
#        placement_policy: cuda
#        force_outputs_fp32: true
#        min_chunk_size: 64

    log_every_n_steps: 2
    logger: True
    default_root_dir: "experiments/"

  logger:
    wandb:
      target: lightning.pytorch.loggers.WandbLogger
      params:
          project: control
          group: ucf101
          name: nowname
          save_dir: "experiments/"
          offline: opt.debug
          id: nowname
